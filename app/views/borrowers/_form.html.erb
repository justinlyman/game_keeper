<%= simple_form_for(@borrower, html: { class: 'form' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :first_name %>
  <%= f.input :last_name %>
  <%= f.button :submit %>
  <%= link_to 'Cancel', borrowers_path, class: 'btn btn-danger' %>
<% end %>