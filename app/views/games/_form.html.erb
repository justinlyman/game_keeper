<%= simple_form_for(@game, html: { class: 'form' }) do |f| %>
  <%= f.error_notification %>
  <%= f.input :name %>
  <%= f.button :submit %>
  <%= link_to 'Cancel', borrowers_path, class: 'btn btn-danger' %>
<% end %>